"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createInternalTheme = void 0;
const createInternalTheme = (foundations) => {
    const res = {};
    const base = foundations;
    for (const scale in base) {
        res[scale] = {};
        for (const shade in base[scale]) {
            res[scale]['$' + shade] = base[scale][shade];
        }
    }
    return Object.freeze(res);
};
exports.createInternalTheme = createInternalTheme;
